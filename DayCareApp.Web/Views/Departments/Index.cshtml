@model IEnumerable<DayCareApp.Web.Entities.Department>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Institution.InstitutionName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DepartmentName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Institution.InstitutionName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DepartmentName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DepartMentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DepartMentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DepartMentId })
        </td>
    </tr>
}

</table>

<div>
    @Html.ActionLink("Back to List", "Index", "InstitutionManager")
</div>

